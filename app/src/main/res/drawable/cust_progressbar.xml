<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/pbEmptyColor"
                android:centerColor="@color/pbEmptyColor"
                android:centerY="1.0"
                android:endColor="@color/pbEmptyColor"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>

                <gradient
                    android:startColor="@color/pbProgressColor"
                    android:centerColor="@color/pbProgressColor"
                    android:centerY="1.0"
                    android:endColor="@color/pbProgressColor"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>